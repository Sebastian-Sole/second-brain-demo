---
description: Read your schedule and answer from it — creates an event only when you ask and approve.
---

Read `brain/tools/calendar.md` and follow it exactly.

The human's input, if any: $ARGUMENTS

<!-- Thin adapter. The real prompt is in brain/tools/ so every agent shares it.
     Don't add instructions here — edit brain/tools/calendar.md instead. -->
