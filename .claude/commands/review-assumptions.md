---
description: Tap-fast pass over the vault's open assumptions — confirm, refute or skip. Confirmed ones become facts; refuted ones are kept as calibration.
---

Read `brain/prompts/review-assumptions.md` and follow it exactly.

A subject, a dimension, or `stale`, if given: $ARGUMENTS

<!-- Thin adapter. The real prompt is in brain/prompts/ so every agent shares it.
     Don't add instructions here — edit brain/prompts/review-assumptions.md instead. -->
