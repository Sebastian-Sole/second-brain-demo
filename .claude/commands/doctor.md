---
description: Check this vault's install and say exactly what to fix.
---

Run `brain/bin/doctor` and read its output back to the human as written — the fix lines are for
them, not for you, so don't paraphrase or summarise them.

The human's input, if any: $ARGUMENTS

<!-- Thin adapter. The real check is brain/bin/doctor so every agent shares it.
     Don't add instructions here — edit brain/bin/doctor instead. -->
