# Working state

- **0001 where-a-rule-lives** — delivered 2026-08-20. Covers the three channels, the per-session vs
  per-turn budget, the compaction matcher, the source-of-truth constraint, hook house style.
  Retrieval question asked in conversation; answer not yet recorded.

## Queued

- **0002 friction as a signal** — magic words as a zero-ambiguity detector, the `sync` push hazard,
  what a log line needs to be useful later (pointer to context, not just the word).
- **0003 a decline is evidence** — the register pattern applied to proposals, and the threshold that
  separates memory from nagging.

## Open threads

- AGENTS.md:48 and AGENTS.md:112 use "paid for" in two different senses. Flagged in lesson 1. Not
  yet fixed in the manual.
- Codex / Gemini pre-prompt hook parity: unknown. Bounds the portability claim.
