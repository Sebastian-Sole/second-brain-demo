# Working state

- **0001 where-a-rule-lives** — delivered 2026-08-20. Three channels, per-session vs per-turn
  budget, the compaction matcher, source-of-truth constraint, hook house style.
  Retrieval question asked; **not answered** — he moved to 2 and 7 instead. Revisit.
- **0002 friction-as-a-signal** — delivered 2026-08-20, after a "huh?" on the first telling.
- **0003 a-decline-is-evidence** — delivered 2026-08-20, same reason.

## What the "huh?" was actually about

I said the friction log should live "outside the repo" and he read that as contradicting the whole
persistent-notes pitch. **He was right.** The miss was mine: I never separated the raw line from the
distilled conclusion, so "keep it out" sounded like "corrections are not knowledge". Both belong in
the cortex; only the raw end sits outside the *retrieval path*, in `cortex/raw/`.

**Teaching lesson for next time: he reasons in pipelines.** The explanation landed the moment 2 was
reframed as the raw end of the pipeline whose far end is 6, rather than as a standalone feature.
Lead with where a thing sits in a flow he already accepted.

## Open threads

- AGENTS.md:48 vs AGENTS.md:112 use "paid for" in two senses. Flagged, not fixed.
- **The scope question in lesson 2 is unresolved and blocks implementation of 2.** Does "something
  they *said*" (AGENTS.md:159) mean in-session, or ever-and-provably? His call, needs to land in
  the manual before any script is written.
- Codex / Gemini pre-prompt hook parity: still unknown. Bounds the portability claim.
