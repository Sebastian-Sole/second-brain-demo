#!/usr/bin/env sh
# Retired. The human's writing-style lines live under "How to talk to me" in
# cortex/03_Resources/How we work together.md and are re-stated on every prompt by
# brain/bin/agreement. This file is kept only so old settings.json hooks that still
# point here stay silent instead of erroring; it is safe to delete.
exit 0
