---
description: First run: check the install, learn who you are, make this vault yours.
---

Read `AGENTS.md` in the vault folder, then read `brain/prompts/setup.md` and follow it exactly.

Both files live in the folder attached to this Cowork project — not in this plugin. If you can't
find them, the project has no vault folder attached, or it's attached to the wrong directory. Say
so plainly rather than improvising a second brain from scratch.

The human's input, if any: $ARGUMENTS

<!-- Thin adapter. The real prompt is in brain/prompts/ so every agent shares it.
     Don't add instructions here — edit brain/prompts/setup.md in the vault instead. -->
